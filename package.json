{
  "name": "online-library",
  "version": "1.0.0",
  "private": true,
  "description": "Static online library project",
  "scripts": {
    "dev": "live-server --open=pages/elib1.html --port=8000",
    "start": "npm run dev"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
